const frutas = ['platano', 'manzana', 'platano', 'pera']
const dinero = 1000

// module.exports = frutas

module.exports = {
    frutas: frutas,
    dinero
} // si es redundante (mismo nombre) se puede usar sin : ej, dinero
